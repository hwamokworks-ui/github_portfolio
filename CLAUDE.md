# CLAUDE.md

> **이 프로젝트는 코드를 작성하지 않는다.** GitHub MCP · Notion MCP · Vercel MCP · Resend MCP 도구 호출과 프롬프트(자연어 지시·판단)만으로 모든 작업을 수행한다. 스크립트 파일, 프로그램, 자동화 코드를 새로 작성하지 말 것.
>
> 아래 수치·규칙은 `/sync-repos`를 포함해 이 프로젝트에서 수행하는 **모든 동기화/분석 작업과 재실행의 공통 기준**이다. 임의로 다른 숫자나 기준으로 대체하지 말고, 여기 적힌 값을 그대로 적용한다. (근거: `prd.md`)

## 스키마 — Notion 데이터베이스 "GitHub 리포지토리 관리" (속성 11개)

| 속성명 | 타입 | 비고 |
|---|---|---|
| 저장소명 | title | GitHub 저장소 이름. 중복 방지 조회 키로 사용 |
| Git URL | url | 저장소 URL |
| 생성일 | date | GitHub `created_at` |
| 설명 | rich_text | GitHub description. **재실행할 때마다 전체 저장소 대상으로 최신값 갱신** (아래 "설명" 규칙 참고) |
| 분석상태 | select | 옵션: `대기` / `완료` / `실패` |
| 마지막푸시일 | date | GitHub `pushed_at` |
| 기술태그 | multi_select | 리포트의 [기술스택]에서 추출 |
| 활동상태 | select | 옵션: `방치됨` / `활발함` |
| 포트폴리오 노출 | checkbox | 기존 저장소의 값은 그대로 유지한다. **신규로 추가되는 저장소만** 기본값 미체크(false)로 생성한다 |
| 커버이미지 | files | GitHub OG 이미지 외부 URL |
| Vercel URL | url | Vercel에 실제 배포된 경우의 프로덕션 도메인. 없으면 비워둠 |

## 리포트 형식 (각 저장소 페이지 본문에 이 4개 섹션을 이 이름 그대로 작성)

```
[기술스택]
[추정 핵심기능]
[구조 관찰]
[구조 다이어그램]
```

## 규칙

### 활동상태
`마지막푸시일`이 **오늘 기준 6개월 이상** 지났으면 `방치됨`, 그렇지 않으면 `활발함`으로 판정한다.

### 포트폴리오 노출 및 카드 순서 (기존 값 보존 원칙)
- **기존 저장소의 `포트폴리오 노출` 값과 정렬 순서(카드 순서 기준 뷰의 수동 정렬)는 재실행 시 절대 건드리지 않는다.** 사용자가 Notion 화면에서 직접 체크/해제하거나 드래그로 재배치한 결과이므로, `/sync-repos`·`/build-log` 어느 쪽도 이 값을 임의로 되돌리거나 재조정하지 않는다.
- **신규로 발견된 저장소만** Notion에 새 행으로 추가하고, 이때 `포트폴리오 노출`은 **미체크(false)** 로 생성한다 (자동으로 체크하지 않는다). Notion은 새 페이지를 정렬 조건 없는 뷰의 맨 끝에 추가하므로, 별도 조작 없이 신규 저장소는 자연히 **카드 순서 기준 뷰(`https://app.notion.com/p/by1927/fe68680f5a474f6892ef6d39e3663239?v=53bf797693054bc7b6608a4db91bebd3`)의 가장 하단**에 위치한다.
- GitHub 원본이 삭제된 것이 확인된 저장소만 예외로 `포트폴리오 노출`을 false로 되돌린다 (아래 "포트폴리오 갤러리 카드" 3번 참고) — 이건 "기존 값 보존" 원칙의 유일한 예외다.
- 사용자가 이미 노출을 켠 저장소를 그대로 두는 것은 물론, 사용자가 의도적으로 꺼둔 저장소도 재실행 때마다 다시 켜지 않는다.

### 커버이미지
`https://opengraph.githubassets.com/1/{소유자}/{저장소명}` 을 외부 파일로 `커버이미지` 속성에 등록한다.

### 설명
- GitHub `description`을 그대로 사용하고, 비어있으면(null) `정보 부족`으로 채운다.
- 신규 등록 시에만 채우고 끝내지 않는다 — **Vercel URL과 동일한 원칙으로, 재실행할 때마다 전체 저장소(기존 포함)를 대상으로 GitHub `description`을 다시 조회해 `설명`을 최신값으로 덮어쓴다.** 사용자가 나중에 GitHub 저장소 설정에서 description을 채우거나 수정했을 수 있기 때문이다.
- 이 갱신은 `분석상태`·리포트 본문([기술스택] 등 4개 섹션)·`포트폴리오 노출`에는 영향을 주지 않는다 — `설명` 속성 값만 대상이다.

### Vercel URL
- Vercel MCP `list_teams`로 팀을 확인하고 `list_projects`/`get_project`로 전체 프로젝트의 `domains`·`latestDeployment.readyState`를 가져온다.
- Notion 저장소명과 Vercel 프로젝트명을 하이픈/언더스코어 차이는 무시하고 대조한다.
- 매칭되면 `domains` 중 팀 슬러그가 붙지 않은 가장 짧고 깨끗한 `*.vercel.app` 도메인을 `Vercel URL`에 채운다 (배포가 `ERROR` 상태여도 있는 도메인은 그대로 채운다 — 실제로 안 열릴 수 있다는 점만 별도로 인지하고 넘어간다).
- 매칭되는 Vercel 프로젝트가 없으면 `Vercel URL`은 비워둔다 (기존 값이 있었다면 지운다).
- 이 대조는 **재실행할 때마다 전체 저장소에 대해 다시 수행**한다 (새로 배포되거나, 있던 배포가 사라졌을 수 있다).

### 포트폴리오 갤러리 카드
`포트폴리오 노출`이 체크된 **모든** 저장소는 아래를 둘 다 적용해야 갤러리 카드에 이미지가 실제로 보인다 (파일 속성인 `커버이미지`만으로는 Notion이 미리보기 생성에 실패해 빈 회색 카드로 남는 경우가 있었다):
1. `커버이미지` 파일 속성은 그대로 등록한다 (위 규칙대로).
2. **추가로** 각 페이지의 **네이티브 페이지 커버**를 `update_page`의 `cover` 파라미터로 같은 `https://opengraph.githubassets.com/1/{소유자}/{저장소명}` URL로 설정한다.
3. GitHub 원본이 404(삭제됨)로 확인된 저장소만 `포트폴리오 노출`을 false로 해제하고 페이지 커버는 그대로 둔다.

"포트폴리오" 갤러리 뷰가 없으면 새로 만들고, 있으면 아래 설정을 다시 적용한다:
- `FILTER "포트폴리오 노출" = true; SORT BY "마지막푸시일" DESC; SHOW "저장소명", "설명", "기술태그", "마지막푸시일"`
- `COVER "커버이미지" SIZE large ASPECT cover`

**수동 설정 필요 (최초 1회)**: 갤러리 카드의 미리보기 소스를 "페이지 커버"로 쓰는 설정은 Notion API로 바꿀 수 없다. 뷰가 아직 "커버이미지" 속성 기준으로 되어 있으면, 사용자에게 Notion 화면에서 해당 뷰 `···` → 레이아웃 → 카드 미리보기를 **"페이지 커버"**로 바꿔달라고 안내한다. 한 번 바꿔두면 이후 재실행 시에는 각 페이지의 페이지 커버만 갱신하면 된다.

### 배치 처리
대상 저장소가 **10개를 넘으면 10개씩 묶어서** 처리한다. 한 묶음(배치) 처리가 끝날 때마다 사용자에게 진행 상황을 보고한다 (예: "1~10번째 묶음 처리 완료, 다음 묶음 진행").

### 중복 방지
Notion에 새로 등록/삽입하기 전에 **저장소명**으로 먼저 기존 행을 조회한다. 이미 존재하면 새로 만들지 않고 건너뛴다.

### 다이어그램
- Mermaid `flowchart`(`graph TD`) 문법을 사용한다.
- 모든 노드 라벨은 큰따옴표(`"`)로 감싼다. 예: `A["src"] --> B["components"]`
- Notion 코드 블록의 언어는 반드시 `mermaid`로 지정한다.

### 근거 규칙
README나 폴더 구조 등에서 확인할 수 없는 내용은 **절대 추측하지 않는다**. 근거가 없으면 해당 항목에 `정보 부족`이라고 그대로 적는다.

### 에러 처리
- Notion API가 **429(요청 과다)** 를 반환하면 **10초 대기 후 같은 묶음(배치)을 재시도**한다.
- 이 재시도는 **배치당 최대 3회**까지 허용한다.
- 특정 저장소가 **3회 연속 실패**하면, 해당 저장소의 `분석상태`를 `실패`로 남기고 다음 저장소로 넘어간다 (전체 실행을 중단하지 않는다).

### 보고 형식
작업이 끝나면 마지막에 아래 항목을 **표로** 정리해서 보고한다.

| 전체 | 신규 등록 | 분석 완료 | 건너뜀 | 실패 |
|---|---|---|---|---|
| N | N | N | N | N |

## 참고
- 대상 Notion 데이터소스: `collection://e47e4d32-3ee6-49c8-a343-1c7ab4f1159a` (부모 페이지: `https://app.notion.com/p/3b34a3b69a328016810bd2147f3222c0`)
- "포트폴리오" 갤러리 뷰: `view://3b34a3b6-9a32-81ee-a8de-000c2138e117`
- 카드 순서 기준 뷰(정렬 없는 테이블 뷰, 수동 정렬 유지): `https://app.notion.com/p/by1927/fe68680f5a474f6892ef6d39e3663239?v=53bf797693054bc7b6608a4db91bebd3`
- Vercel 팀: `hwamokworks-4351's projects` (필요시 `list_teams`로 재확인)
- 완료 알림 이메일 수신처: `hwamokworks@gmail.com`
- 상세 배경·범위·타임박스는 `prd.md` 참고
