# CLAUDE.md

> **코드를 작성하지 않는다.** GitHub MCP · Notion MCP · Vercel MCP · Resend MCP 도구 호출과 프롬프트(자연어 지시·판단)만으로 작업한다. 스크립트·프로그램·자동화 코드를 새로 만들지 않는다.
>
> 아래 수치·규칙은 이 프로젝트의 모든 동기화/분석 작업(현재는 `/sync-repos`)과 그 재실행이 항상 지켜야 하는 공통 기준이다. 임의로 다른 값으로 대체하지 않는다. 각 명령의 실행 절차(어떤 도구를 어떤 순서로 부르는지)는 `.claude/commands/*.md`가 원본이고, 왜 이렇게 정했는지는 `prd.md`가 원본이다 — 이 문서는 "항상 참이어야 하는 값과 규칙"만 담는다.

## 스키마 — Notion 데이터베이스 "GitHub 리포지토리 관리" (속성 11개)

| 속성명 | 타입 | 비고 |
|---|---|---|
| 저장소명 | title | GitHub 저장소 이름. 중복 방지 조회 키로 사용 |
| Git URL | url | 저장소 URL |
| 생성일 | date | GitHub `created_at` |
| 설명 | rich_text | GitHub description. 재실행할 때마다 전체 저장소 대상으로 최신값 갱신 |
| 분석상태 | select | 옵션: `대기` / `완료` / `실패` |
| 마지막푸시일 | date | GitHub `pushed_at` |
| 기술태그 | multi_select | 리포트의 [기술스택]에서 추출 |
| 활동상태 | select | 옵션: `방치됨` / `활발함` |
| 포트폴리오 노출 | checkbox | 기존 값은 그대로 유지. 신규 저장소만 기본값 미체크(false)로 생성 |
| 커버이미지 | files | GitHub OG 이미지 외부 URL |
| Vercel URL | url | 실제 배포된 경우의 프로덕션 도메인. 없으면 비워둠 |

## 리포트 형식 (각 저장소 페이지 본문에 이 4개 섹션을 이 이름 그대로 작성)

```
[기술스택]
[추정 핵심기능]
[구조 관찰]
[구조 다이어그램]
```

## 핵심 규칙

### 활동상태
`마지막푸시일`이 오늘 기준 **6개월 이상** 지났으면 `방치됨`, 아니면 `활발함`.

### 포트폴리오 노출·카드 순서 — 기존 값 보존
- 기존 저장소의 `포트폴리오 노출` 값과 카드 순서 기준 뷰(수동 정렬)는 재실행 시 절대 건드리지 않는다 — 켜둔 것도 꺼둔 것도 그대로 둔다.
- 신규 저장소만 `포트폴리오 노출=false`로 생성한다. Notion이 새 페이지를 정렬 없는 뷰의 맨 끝에 붙이므로, 별도 조작 없이 카드 순서 기준 뷰(`https://app.notion.com/p/by1927/fe68680f5a474f6892ef6d39e3663239?v=53bf797693054bc7b6608a4db91bebd3`) 맨 하단에 위치한다.
- 유일한 예외: GitHub 원본이 삭제(404) 확인된 저장소는 `포트폴리오 노출`을 false로 되돌리고 `분석상태=실패`, `커버이미지`는 비운다. Notion API에는 페이지 삭제가 없으므로 완전 삭제는 사용자가 화면에서 직접 한다.

### 설명 / Vercel URL — 재실행마다 전체 재확인
둘 다 신규·기존 구분 없이 매 실행 전체 저장소를 다시 조회해 최신값으로 덮어쓴다(사용자가 GitHub에서 description을 채우거나, 새로 배포/배포 해제했을 수 있다). 이 갱신은 `분석상태`·리포트 본문·`포트폴리오 노출`에는 영향을 주지 않는다.
- **설명**: GitHub `description`, 없으면 `정보 부족`.
- **Vercel URL**: 저장소명과 Vercel 프로젝트명을 하이픈/언더스코어 차이 무시하고 대조. 매칭되면 팀 슬러그 없는 가장 짧고 깨끗한 `*.vercel.app` 도메인(배포가 `ERROR` 상태여도 도메인이 있으면 채움). 매칭 없으면 비운다(기존 값 있어도 지운다).

### 커버이미지 — 파일 속성 + 네이티브 커버 이중 등록
`https://opengraph.githubassets.com/1/{소유자}/{저장소명}`을 두 곳에 등록해야 갤러리 카드 미리보기가 실제로 뜬다 (파일 속성만으로는 빈 회색 카드로 남는 경우가 있었다):
1. `커버이미지`(files) 속성에 외부 URL로 등록.
2. `포트폴리오 노출=true`인 **모든** 저장소는 같은 URL을 `update_page`의 `cover` 파라미터로 페이지 네이티브 커버에도 설정/갱신(노출 false면 건너뜀).

대시보드 뷰("포트폴리오" 갤러리, "기술스택 분포", "활동상태 비율")의 정확한 스펙은 `.claude/commands/sync-repos.md` 6단계를 따른다. 갤러리 카드 미리보기 소스를 "페이지 커버"로 쓰는 설정은 Notion 화면에서 최초 1회 사용자가 직접 바꿔야 한다(API로 불가) — 아직 안 바뀌어 있으면 안내한다.

### 배치·재시도
- 대상이 10개를 넘으면 10개씩 묶어 처리하고, 묶음마다 진행 상황을 보고한다.
- Notion 429 → 10초 대기 후 같은 배치 재시도(배치당 최대 3회). 저장소 하나가 3회 연속 실패하면 그 저장소만 `분석상태=실패`로 남기고 계속 진행한다(전체 중단 금지).

### 중복 방지
Notion에 새로 등록하기 전 **저장소명**(title, Git URL 아님)으로 기존 행을 조회한다. 있으면 건너뛰고, 없는 것만 생성한다.

### 다이어그램
Mermaid `flowchart`(`graph TD`). 모든 노드 라벨을 큰따옴표로 감싼다 (예: `A["src"] --> B["components"]`). Notion 코드 블록 언어는 `mermaid`.

### 근거 규칙
README·폴더 구조 등에서 확인할 수 없는 내용은 추측하지 않는다 — `정보 부족`이라고 그대로 적는다.

### 보고 형식
작업이 끝나면 마지막에 아래 표로 정리해서 보고한다.

| 전체 | 신규 등록 | 분석 완료 | 건너뜀 | 실패 |
|---|---|---|---|---|
| N | N | N | N | N |

## 참고
- 대상 Notion 데이터소스: `collection://e47e4d32-3ee6-49c8-a343-1c7ab4f1159a` (부모 페이지: `https://app.notion.com/p/3b34a3b69a328016810bd2147f3222c0`)
- "포트폴리오" 갤러리 뷰: `view://3b34a3b6-9a32-81ee-a8de-000c2138e117`
- 카드 순서 기준 뷰(정렬 없는 테이블 뷰, 수동 정렬 유지): `https://app.notion.com/p/by1927/fe68680f5a474f6892ef6d39e3663239?v=53bf797693054bc7b6608a4db91bebd3`
- Vercel 팀: `hwamokworks-4351's projects` (필요시 `list_teams`로 재확인)
- 완료 알림 이메일 수신처: `hwamokworks@gmail.com`
- 배경·DoD·Out of Scope·리스크·변경이력은 `prd.md` 참고
