# PRD: `/sync-repos` — GitHub 리포지토리 → Notion 포트폴리오 자동 정리

- 작성일: 2026-08-05 (최종 갱신: 2026-08-15 — 아래 각주 없는 수치·규칙은 모두 최신 실제 동작 기준으로 갱신됨)
- 작성 대상: 비개발자 학습자, 실습 시간 3시간(최초 MVP 기준. 이후 여러 차례 재실행하며 범위가 확장됨)
- 실행 방식: 코드 작성 없음. GitHub MCP · Notion MCP · Vercel MCP · Resend MCP 도구 호출 + 프롬프트(자연어 지시)만으로 동작하는 **재사용 가능한 슬래시 명령**
- 관련 기존 산출물: Notion 데이터베이스 "GitHub 리포지토리 관리" (부모 페이지: `https://app.notion.com/p/3b34a3b69a328016810bd2147f3222c0`, 데이터소스 ID `collection://e47e4d32-3ee6-49c8-a343-1c7ab4f1159a`)

---

## 1. 배경 및 문제

GitHub에 흩어진 개인 저장소들을 한눈에 파악할 방법이 없다. 어떤 저장소가 포트폴리오로 내세울 만큼 활발한지, 어떤 저장소는 방치되어 있는지, 기술 스택은 무엇인지를 저장소 하나하나 열어보지 않고는 알 수 없다. 이를 Notion 데이터베이스로 자동 정리하고, 저장소별 요약 리포트와 시각화(차트/갤러리)까지 한 번의 명령으로 구성하고자 한다.

## 2. 목표 (3시간 MVP 기준)

1. 실행자가 `/sync-repos` 한 번 호출로 **GitHub 계정의 저장소 목록 → Notion DB 반영 → 저장소별 요약 리포트 → 활동 분류 → Vercel 배포 확인 → 포트폴리오 노출 선정 → 차트/갤러리 뷰 구성 → 완료 이메일 발송**까지 이어지는 과정을 확인한다.
2. 비개발자도 명령 1줄 실행 결과를 보고 "무엇이 어떻게 채워졌는지" 바로 이해할 수 있어야 한다.
3. 재실행 시 **저장소명 기준**으로 중복 행을 만들지 않는다. Vercel 배포 상태는 재실행할 때마다 전체 저장소를 대상으로 다시 확인해 최신 상태로 갱신하지만, **`포트폴리오 노출` 값과 카드 정렬 순서는 기존 저장소에 한해 절대 건드리지 않는다** — 사용자가 Notion에서 직접 체크/드래그로 정해둔 상태를 그대로 보존한다. 신규로 발견된 저장소만 노출 미체크(false) 상태로, 정렬 순서 맨 끝에 추가된다.

### 성공 지표 (Definition of Done)
- [ ] Notion DB에 대상 저장소 수만큼 행이 생성되어 있다 (재실행 시 이미 있는 저장소명은 새로 만들지 않는다).
- [ ] 각 행(페이지) 본문에 기술스택 / 추정 핵심기능 / 구조 관찰 / 구조 다이어그램(Mermaid) 4개 섹션이 채워져 있다.
- [ ] `활동상태` 속성이 기준에 따라 방치됨/활발함으로 자동 채워져 있다.
- [ ] Vercel에 실제 배포된 저장소는 `Vercel URL` 속성에 프로덕션 도메인이 채워져 있다 (재실행마다 전체 재확인).
- [ ] **기존** 저장소의 `포트폴리오 노출` 값과 카드 순서는 재실행 전후로 변하지 않는다 (GitHub 원본이 삭제 확인된 저장소만 예외로 노출이 꺼짐). **신규** 저장소는 노출 미체크 상태로 맨 끝에 추가된다.
- [ ] DB에 차트 뷰 2개("기술스택 분포", "활동상태 비율")와 갤러리 뷰 1개("포트폴리오")가 존재한다.
- [ ] 실행 완료 후 `hwamokworks@gmail.com`으로 요약 완료 메일이 1통 수신된다.

## 3. 범위 (Scope)

### 3.1 In Scope (최신 실제 동작 기준)
| 영역 | 내용 |
|---|---|
| 대상 저장소 | 인증된 GitHub 계정의 **전체 저장소 (public + private, fork 포함)**. 개수 제한이나 `--limit` 같은 인자는 없다 — 페이지네이션 끝까지 전량 조회한다 |
| 저장소 조회 | 이름, Git URL, 생성일, 설명, `pushed_at` |
| 콘텐츠 분석 | README 원문 + 최상위/2단계 폴더·파일 목록(트리)만 조회. 파일 내용 전체 스캔은 하지 않음 |
| 리포트 항목 | ① 기술스택(추정) ② 추정 핵심기능 ③ 구조 관찰 ④ 구조 다이어그램(Mermaid 코드블록) |
| 활동 분류 | 마지막 푸시일 기준 규칙 기반 분류 (기준은 FR-5 참조) |
| Vercel 배포 확인 | Vercel MCP로 전체 프로젝트를 조회해 Notion 저장소명과 대조하고, 매칭되면 `Vercel URL`에 프로덕션 도메인을 채운다. **재실행할 때마다 전체 저장소를 다시 대조**한다 (새 배포·삭제된 배포 반영) |
| 포트폴리오 노출·카드 순서 | **기존 저장소는 값과 정렬 순서를 절대 건드리지 않는다** (사용자가 Notion에서 직접 정한 상태 보존). 신규 저장소만 노출 미체크(false)로, 정렬 기준 뷰의 맨 끝에 추가. GitHub 원본이 삭제 확인된 저장소만 예외로 노출 해제 |
| 커버이미지 | GitHub OG 이미지 URL(`https://opengraph.githubassets.com/1/{owner}/{repo}`)을 외부 파일로 `커버이미지` 속성에 등록 + 같은 URL을 페이지 **네이티브 커버**로도 설정(갤러리 카드 썸네일이 파일 속성만으로는 안 뜨는 경우가 있어 이중 등록) |
| 뷰 구성 | 차트 뷰 2개("기술스택 분포" 막대, "활동상태 비율" 도넛) + 갤러리 뷰 1개("포트폴리오": 포트폴리오 노출=true 필터, 커버이미지 표시) |
| 배치·재시도 | 대상이 10개 초과면 10개씩 묶어 처리하고 묶음마다 진행 보고. Notion 429 발생 시 10초 대기 후 같은 묶음 재시도(배치당 최대 3회), 특정 저장소가 3회 연속 실패하면 그 저장소만 `분석상태=실패`로 남기고 계속 진행 |
| 부모 페이지 요약 | 부모 페이지("포트폴리오 관리")의 `## 포트폴리오 요약` 섹션을 분석 완료된 리포트 내용 기준으로 매번 최신화(없으면 새로 삽입, 있으면 덮어쓰기) |
| 알림 | Resend MCP로 실행 결과 요약(전체/신규 등록/분석 완료/건너뜀/실패 개수, Vercel URL 채워진 저장소 개수, 실패 사유)을 본인 이메일로 발송 |
| 재실행 | Notion **저장소명(title)** 기준으로 기존 행을 조회해 이미 있으면 새로 만들지 않고 건너뛴다 (URL이 아니라 저장소명이 중복 방지 키). 이미 `분석상태=완료`인 저장소는 리포트 재작성도 건너뛴다 |

### 3.2 Out of Scope
- Fork 저장소 자동 제외/포함 정책 세분화
- 서브 디렉토리 전체 재귀 스캔, 코드 정적 분석(정확한 언어 비율 등은 GitHub API의 `languages` 값으로 대체)
- 자동 스케줄 실행(주기적 cron 동기화) — 여전히 수동 실행만
- Notion 커버이미지 직접 업로드(파일 생성 API) — 외부 URL 참조로 대체
- Resend 발신 도메인 신규 설정 — 기존에 인증된 도메인/주소가 있다는 전제
- 다국어 리포트, 저장소별 상세 커스텀 프롬프트 튜닝
- 오류 발생 저장소에 대한 재시도 로직 고도화(배치당 최대 3회 재시도 이상의 정교한 백오프 등)
- 커스텀 도메인까지 포함한 Vercel 배포 판정(기본 `*.vercel.app` 도메인만 신뢰)

> **범위 변경 이력**: 최초 MVP는 "public 저장소만, 상위 15개, 90일 기준 활동 분류, 상위 5개만 포트폴리오 노출, URL 기준 upsert"로 설계됐으나, 이후 재실행을 거치며 **전체 저장소(private 포함) 전량 조회 / 6개월 기준 활동 분류 / 저장소명 기준 skip-if-exists**로 방향이 바뀌었고, Vercel 배포 확인이 새 핵심 기능으로 추가됐다. 포트폴리오 노출 정책도 한 차례 더 바뀌어 "전체 자동 체크" → **"기존 값 보존 + 신규만 미체크로 추가"**가 됐다(사용자가 Notion에서 직접 정한 노출 여부·카드 순서를 자동화가 되돌리지 않도록). 아래 FR은 이 최신 기준으로 갱신되어 있다.

## 4. 상세 요구사항

### FR-1. 저장소 목록 조회
- GitHub MCP `search_repositories`로 인증된 계정의 **전체 저장소(public + private, fork 포함)**를 `pushed_at` 내림차순으로, 페이지네이션 끝까지 조회한다. 개수 제한 인자는 없다.
- 결과 개수가 사전 점검에서 확인한 예상 개수(`public_repos` + `total_private_repos`)와 다르거나, 30·60·100처럼 딱 떨어지는 값이면 페이지네이션 누락을 의심하고 다음 페이지까지 재조회한다.

### FR-2. Notion DB 신규 등록 (skip-if-exists, upsert 아님)
- 기존 "GitHub 리포지토리 관리" 데이터소스를 대상으로, **저장소명(title)** 으로 기존 행을 조회한다 (`Git URL`이 아니라 저장소명이 중복 방지 키).
- 이미 존재하는 저장소명은 건너뛴다 — 속성을 다시 덮어쓰지 않는다. 없는 것만 새 페이지(행)를 생성한다.
- 채우는 속성: 저장소명(title), `Git URL`, 생성일(GitHub `created_at`), 설명(GitHub `description`, 없으면 "정보 부족"), 마지막푸시일(GitHub `pushed_at`), 분석상태(="대기"), 활동상태(FR-5 규칙), 커버이미지(FR-7), **포트폴리오 노출(="false", 미체크)**.
- Notion이 새 페이지를 정렬 조건 없는 뷰의 맨 끝에 추가하는 특성을 이용해, 신규 저장소는 별도 조작 없이 카드 순서 기준 뷰(`https://app.notion.com/p/by1927/fe68680f5a474f6892ef6d39e3663239?v=53bf797693054bc7b6608a4db91bebd3`, `/build-log`가 카드 순서를 읽어오는 뷰)에서 자연히 맨 하단에 위치한다.

### FR-3. README·구조 분석 및 리포트 생성
- GitHub MCP로 README 원문과 저장소 루트~2단계 디렉토리 트리를 조회한다.
- 프롬프트 기반으로 다음 4개 섹션을 생성해 해당 행 페이지 **본문**에 기록한다.
  1. **기술스택**: README 언급 + GitHub `languages` API 결과 종합
  2. **추정 핵심기능**: README 목적/사용법 서술 기반 요약 (최대 5줄)
  3. **구조 관찰**: 폴더 구성에서 보이는 패턴(예: 모노레포 여부, 테스트/문서 폴더 유무 등)
  4. **구조 다이어그램**: 최상위 2단계 폴더 구조를 Mermaid `graph TD` 코드블록으로 표현
- README가 없는 저장소는 "README 없음"으로 표기하고 구조 정보만으로 리포트를 생성한다(실행 중단하지 않음).

### FR-4. 기술태그 채우기
- FR-3에서 도출한 기술스택 키워드를 `기술태그`(multi_select) 속성에 반영한다. 기존 옵션에 없는 태그는 자동 생성되어 추가된다.

### FR-5. 활동상태 자동 분류
- 기준: `마지막푸시일`이 오늘 기준 **6개월 이상** 지났으면 `방치됨`, 아니면 `활발함`.
- 조정 가능한 인자는 없다 — 이 기준은 CLAUDE.md에 고정 규칙으로 명시되어 있고 임의로 다른 값으로 대체하지 않는다.

### FR-6. 포트폴리오 노출 및 카드 순서 — 기존 값 보존 원칙
- **기존 저장소의 `포트폴리오 노출` 값과 카드 순서(정렬 기준 뷰의 수동 정렬)는 재실행 시 절대 변경하지 않는다.** 사용자가 Notion 화면에서 직접 체크/해제하거나 드래그로 재배치한 결과이므로, 이미 켜진 것도 꺼둔 것도 그대로 유지한다.
- 신규로 발견된 저장소는 FR-2에서 이미 `포트폴리오 노출=false`로 생성되므로 이 단계에서 별도로 켜지 않는다.
- 유일한 예외: GitHub 원본이 404(삭제됨)로 확인된 저장소는 `포트폴리오 노출`을 false로 해제하고, `분석상태=실패`·`커버이미지=null`로 처리한다. Notion API에는 페이지 삭제 기능이 없어 완전 삭제가 필요하면 사용자에게 화면에서 직접 휴지통으로 보내달라고 안내한다.

### FR-7. 커버이미지 자동 채움 (갤러리 카드 노출을 위한 이중 등록)
- `커버이미지`(files) 속성에 `https://opengraph.githubassets.com/1/{owner}/{repo}` 외부 URL을 문자열로 등록한다.
- **추가로** `포트폴리오 노출`이 현재 켜져 있는(true) 저장소는 같은 URL을 `update_page`의 `cover` 파라미터로 **페이지 네이티브 커버**에도 설정/갱신한다 — 파일 속성만으로는 Notion 갤러리 카드 미리보기 생성이 실패해 빈 회색 카드로 남는 사례가 있었기 때문에 둘 다 필요하다. 노출이 꺼진 저장소는 이 단계를 건너뛴다.
- 갤러리 뷰의 카드 미리보기 소스를 "페이지 커버"로 쓰는 설정은 Notion API로 바꿀 수 없다 — 최초 1회는 사용자가 화면에서 뷰 `···` → 레이아웃 → 카드 미리보기를 "페이지 커버"로 직접 바꿔야 한다.

### FR-8. Vercel 배포 확인 (신규 — 재실행마다 전체 재수행)
- Vercel MCP `list_teams`로 팀을 확인하고, `list_projects`/`get_project`로 전체 프로젝트의 `domains`·`latestDeployment.readyState`를 가져온다.
- Notion 저장소명과 Vercel 프로젝트명을 하이픈/언더스코어 차이는 무시하고 대조한다.
- 매칭되면 `domains` 중 팀 슬러그가 붙지 않은 가장 짧고 깨끗한 `*.vercel.app` 도메인을 `Vercel URL`에 채운다 (`readyState`가 `ERROR`여도 도메인이 있으면 그대로 채운다 — 실제로 안 열릴 수 있다는 점만 인지하고 넘어간다).
- 매칭되는 Vercel 프로젝트가 없으면 `Vercel URL`을 비워둔다 (기존 값이 있었다면 지운다 — 배포가 사라졌을 수 있으므로).
- 이 대조는 URL 기준 정적 매칭이 아니라 **재실행할 때마다 전체 저장소에 대해 다시 수행**한다.

### FR-9. 차트/갤러리 뷰 구성
- **갤러리 뷰 — "포트폴리오"**: `포트폴리오 노출 = true` 필터, `마지막푸시일` 내림차순 정렬, 카드 커버로 `커버이미지` 사용(SIZE large, ASPECT cover).
- **차트 뷰 1 — "기술스택 분포"**: `기술태그` multi_select 기준 막대 차트.
- **차트 뷰 2 — "활동상태 비율"**: `활동상태` select 기준 도넛 차트.
- 뷰는 이름으로 존재 여부를 판단해 이미 있으면 그대로 두고(설정만 다시 적용), 없는 것만 새로 만든다 — 동일 이름 뷰 중복 생성 금지.

### FR-10. 부모 페이지 요약 섹션 갱신 (신규)
- 부모 페이지("포트폴리오 관리")에 `## 포트폴리오 요약`으로 시작하는 섹션이 있으면 최신 내용으로 통째로 교체하고, 없으면 데이터베이스 블록보다 위에 새로 삽입한다.
- 분석 완료된 저장소들의 실제 리포트 내용만 근거로 한 줄 자기소개, 요약 지표(전체/완료/기술 종류 수/최근 활동 수), 기술스택 개요(프론트/백/데이터/기타)를 작성한다. 추측해서 지어내지 않는다.

### FR-11. 완료 알림 이메일
- 모든 저장소 처리가 끝나면 Resend MCP로 `hwamokworks@gmail.com`에 요약 메일을 발송한다.
- 메일 본문 최소 포함 항목: 전체/신규 등록/분석 완료/건너뜀/실패 개수, `Vercel URL`이 채워진 저장소 개수(= 실제 배포 확인된 개수), 실패한 저장소가 있다면 사유, Notion DB 링크.
- 이메일 발송은 **모든 저장소 처리가 끝난 마지막 단계**에서 정확히 1회만 실행한다(저장소별 반복 발송 금지).

### FR-12. 배치 처리 및 오류 재시도
- 대상 저장소가 10개를 넘으면 10개씩 묶어 처리하고, 묶음마다 진행 상황을 사용자에게 보고한다.
- Notion API가 429를 반환하면 10초 대기 후 같은 배치를 재시도한다(배치당 최대 3회). 특정 저장소가 3회 연속 실패하면 그 저장소만 `분석상태=실패`로 남기고 다음 저장소로 진행한다 — 전체 실행을 중단하지 않는다.
- 속성 업데이트 호출이 성공 응답을 반환해도 실제로 반영되지 않을 수 있으므로, 배치 처리 후 몇 건은 `notion-fetch`로 직접 열어 검증한다 (SQL 쿼리만으로 검증하지 않는다 — 캐시 지연이 있을 수 있다).

### FR-13. 슬래시 명령 사양
- 이름: `/sync-repos`
- 인자: 없음 (`argument-hint: ""`) — 조정 가능한 옵션은 현재 제공하지 않는다.
- 실행 주체: Claude Code 세션 (사람이 명령만 입력, 이후 전 과정 자동 진행)
- 사용 도구: GitHub MCP(저장소 목록/README/트리 조회), Notion MCP(DB 조회·페이지 생성/본문 작성·뷰 생성·요약 섹션 갱신), Vercel MCP(팀/프로젝트/도메인 조회), Resend MCP(메일 발송)
- 실행 중 진행 상황을 단계별로 사용자에게 짧게 보고한다(예: "1~10번째 묶음 처리 완료, 다음 묶음 진행").

## 5. 사전 조건 / 리스크

| 항목 | 상태 | 비고 |
|---|---|---|
| GitHub MCP | 연결됨 | `claude mcp list`에서 Connected 확인 |
| Notion MCP | 연결됨 | 인증 완료, 대상 DB 생성 완료 |
| Vercel MCP | 연결 확인 필요 | `list_teams`로 팀 접근 가능 여부를 매 실행 사전 점검한다. 프로젝트가 0개로 보이면 다른 계정에 연결된 것이므로 사용자에게 재인증(`/mcp`)을 요청하고 대기한다 |
| Resend MCP | 연결됨 | 연결 안 되어 있으면 실행 자체를 중단하고 사용자에게 알린다(메일 발송이 마지막 단계에 필수이기 때문) |
| GitHub API 사용량 제한 | 낮은 위험 | 전체 저장소를 조회하지만 페이지네이션으로 나눠 요청하며, 대량 응답은 토큰 한도를 넘기지 않도록 `minimal_output`/`Grep` 조합으로 처리한다 |
| Notion 차트 뷰 지원 | 확인 필요 | 워크스페이스 플랜에 따라 차트 뷰 기능 제공 여부가 다를 수 있음 — 실행 시 미지원이면 표(테이블) 뷰 + 안내 문구로 대체 |
| Mermaid 다이어그램 렌더링 | 낮은 위험 | Notion이 코드블록을 있는 그대로 보여줄 수 있음(워크스페이스에 따라 실제 다이어그램 렌더링 미지원 가능) — 코드로 남기는 것 자체가 목표이므로 실행엔 지장 없음 |

## 6. 3시간 타임박스 계획 (최초 MVP 실행 당시 기록 — 이후 여러 차례 재실행되며 위 FR 상당수가 이 계획 이후에 추가/변경됨)

| 시간 | 작업 |
|---|---|
| 0:00–0:20 | Resend MCP 연결/인증, 기존 Notion DB·GitHub 연결 상태 재확인 |
| 0:20–1:00 | `/sync-repos` 프롬프트 초안 작성 및 저장소 3~5개로 소규모 시험 실행 |
| 1:00–1:40 | README/구조 리포트 품질 확인 및 프롬프트 보정, 활동 분류·포트폴리오 선정 로직 검증 |
| 1:40–2:10 | 차트 뷰 2개 + 갤러리 뷰 1개 구성 |
| 2:10–2:30 | Resend 이메일 발송 연동 및 실제 수신 확인 |
| 2:30–2:50 | 전체 저장소(최대 15개) 대상 풀 실행 |
| 2:50–3:00 | 결과 점검, Definition of Done 체크리스트 확인 |

## 7. 향후 확장 아이디어 (이번 범위 밖)
- 조직(org) 소유 저장소 포함 여부 옵션화 (현재는 개인 계정 소유 저장소만 대상)
- 정기 자동 동기화(스케줄링)
- 저장소별 커밋 활동 그래프까지 포함한 더 정교한 활동 점수화
- 실패한 저장소에 대한 배치당 3회 재시도 이상의 정교한 재시도/원인별 알림 세분화
- 커스텀 도메인까지 포함하도록 Vercel 배포 판정 기준 보강 (현재는 `*.vercel.app` 기본 도메인만 신뢰)

> ~~Private 저장소 지원~~ — 이미 반영됨 (FR-1 참고, 전체 저장소를 public/private 구분 없이 조회한다).
