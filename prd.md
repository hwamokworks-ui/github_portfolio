# PRD: `/sync-repos` — GitHub 리포지토리 → Notion 포트폴리오 자동 정리

- 작성일: 2026-08-05
- 작성 대상: 비개발자 학습자, 실습 시간 3시간
- 실행 방식: 코드 작성 없음. GitHub MCP · Notion MCP · Resend MCP 도구 호출 + 프롬프트(자연어 지시)만으로 동작하는 **재사용 가능한 슬래시 명령**
- 관련 기존 산출물: Notion 데이터베이스 "GitHub 리포지토리 관리" (부모 페이지: `https://app.notion.com/p/3b34a3b69a328016810bd2147f3222c0`, 데이터소스 ID `collection://e47e4d32-3ee6-49c8-a343-1c7ab4f1159a`)

---

## 1. 배경 및 문제

GitHub에 흩어진 개인 저장소들을 한눈에 파악할 방법이 없다. 어떤 저장소가 포트폴리오로 내세울 만큼 활발한지, 어떤 저장소는 방치되어 있는지, 기술 스택은 무엇인지를 저장소 하나하나 열어보지 않고는 알 수 없다. 이를 Notion 데이터베이스로 자동 정리하고, 저장소별 요약 리포트와 시각화(차트/갤러리)까지 한 번의 명령으로 구성하고자 한다.

## 2. 목표 (3시간 MVP 기준)

1. 실행자가 `/sync-repos` 한 번 호출로 **GitHub 계정의 저장소 목록 → Notion DB 반영 → 저장소별 요약 리포트 → 활동 분류 → 포트폴리오 노출 선정 → 차트/갤러리 뷰 구성 → 완료 이메일 발송**까지 이어지는 과정을 확인한다.
2. 비개발자도 명령 1줄 실행 결과를 보고 "무엇이 어떻게 채워졌는지" 바로 이해할 수 있어야 한다.
3. 재실행 시 중복 행을 만들지 않고 기존 행을 갱신한다(최소한의 재사용성 확보).

### 성공 지표 (Definition of Done)
- [ ] Notion DB에 대상 저장소 수만큼 행이 생성/갱신되어 있다.
- [ ] 각 행(페이지) 본문에 기술스택 / 추정 핵심기능 / 구조 관찰 / 구조 다이어그램(Mermaid) 4개 섹션이 채워져 있다.
- [ ] `활동상태` 속성이 기준에 따라 방치됨/활발함으로 자동 채워져 있다.
- [ ] 최근 활동 기준 상위 5개 저장소만 `포트폴리오 노출` = 체크됨 상태다.
- [ ] DB에 차트 뷰 2개(기술태그 분포, 활동상태 비율)와 갤러리 뷰 1개(포트폴리오용)가 존재한다.
- [ ] 실행 완료 후 `hwamokworks@gmail.com`으로 요약 완료 메일이 1통 수신된다.

## 3. 범위 (Scope)

### 3.1 In Scope (이번 실습에서 구현)
| 영역 | 내용 |
|---|---|
| 대상 저장소 | 인증된 GitHub 계정의 **public 저장소만**, 최근 push 기준 상위 **최대 15개** (기본값, 인자로 조정 가능) |
| 저장소 조회 | 이름, URL, 생성일, 설명, 기본 브랜치 최근 push 일시 |
| 콘텐츠 분석 | README 원문 + 최상위/2단계 폴더·파일 목록(트리)만 조회. 파일 내용 전체 스캔은 하지 않음 |
| 리포트 항목 | ① 기술스택(추정) ② 추정 핵심기능 ③ 구조 관찰 ④ 구조 다이어그램(Mermaid 코드블록) |
| 활동 분류 | 마지막 푸시일 기준 규칙 기반 분류 (기준은 4.4 참조) |
| 포트폴리오 노출 | 마지막 푸시일 최신순 상위 5개 자동 체크 |
| 커버이미지 | GitHub OG 이미지 URL(`https://opengraph.githubassets.com/1/{owner}/{repo}`)을 외부 파일로 등록 |
| 뷰 구성 | 차트 뷰 2개(기술태그 분포 막대, 활동상태 비율 도넛/원형) + 갤러리 뷰 1개(포트폴리오 노출=true 필터, 커버이미지 표시) |
| 알림 | Resend MCP로 실행 결과 요약(처리 저장소 수, 활발함/방치됨 개수, 포트폴리오 노출 목록)을 본인 이메일로 발송 |
| 재실행 | 기존 행은 URL 기준으로 매칭해 덮어쓰기(upsert), 중복 생성 방지 |

### 3.2 Out of Scope (이번 3시간에서는 제외 — 향후 확장)
- Private 저장소 처리 (권한 스코프 확장 필요)
- Fork 저장소 자동 제외/포함 정책 세분화
- 서브 디렉토리 전체 재귀 스캔, 코드 정적 분석(정확한 언어 비율 등은 GitHub API의 `languages` 값으로 대체)
- 자동 스케줄 실행(주기적 cron 동기화) — 이번엔 수동 1회 실행만
- Notion 커버이미지 직접 업로드(파일 생성 API) — 외부 URL 참조로 대체
- Resend 발신 도메인 신규 설정 — 기존에 인증된 도메인/주소가 있다는 전제
- 다국어 리포트, 저장소별 상세 커스텀 프롬프트 튜닝
- 오류 발생 저장소에 대한 재시도 로직 고도화 (실패 시 목록만 남기고 계속 진행)

## 4. 상세 요구사항

### FR-1. 저장소 목록 조회
- GitHub MCP로 인증된 계정의 public 저장소 목록을 가져온다.
- `pushed_at` 내림차순 정렬 후 상위 N개(기본 15, 슬래시 명령 인자로 override 가능: `/sync-repos --limit 10`)만 대상으로 삼는다.
- 대상에서 제외된 저장소가 있다면 실행 요약에 "제외된 저장소 수"로 표기한다(무엇이 잘렸는지 투명하게).

### FR-2. Notion DB Upsert
- 기존 "GitHub 리포지토리 관리" 데이터소스를 대상으로, `URL` 속성으로 기존 행을 조회한다.
- 존재하면 속성만 갱신, 없으면 새 페이지(행) 생성.
- 채우는 속성: 저장소명(title), URL, 생성일(GitHub `created_at`), 설명(GitHub `description`).

### FR-3. README·구조 분석 및 리포트 생성
- GitHub MCP로 README 원문과 저장소 루트~2단계 디렉토리 트리를 조회한다.
- 프롬프트 기반으로 다음 4개 섹션을 생성해 해당 행 페이지 **본문**에 기록한다.
  1. **기술스택**: README 언급 + GitHub `languages` API 결과 종합
  2. **추정 핵심기능**: README 목적/사용법 서술 기반 요약 (최대 5줄)
  3. **구조 관찰**: 폴더 구성에서 보이는 패턴(예: 모노레포 여부, 테스트/문서 폴더 유무 등)
  4. **구조 다이어그램**: 최상위 2단계 폴더 구조를 Mermaid `graph TD` 코드블록으로 표현
- README가 없는 저장소는 "README 없음"으로 표기하고 구조 정보만으로 리포트를 생성한다(실행 중단하지 않음).

### FR-4. 기술태그 채우기
- FR-3에서 도출한 기술스택 키워드를 `기술태그`(multi_select) 속성에 반영한다. 기존 옵션에 없는 태그는 자동 생성되어 추가된다.

### FR-5. 활동상태 자동 분류
- 기준: `마지막푸시일`이 오늘로부터 **90일 이내**면 `활발함`, 초과하면 `방치됨`.
- 90일 기준은 슬래시 명령 인자로 조정 가능하게 설계한다 (`--active-days 90`).

### FR-6. 포트폴리오 노출 자동 선정
- 이번 실행 대상 저장소 중 `마지막푸시일` 기준 상위 5개만 `포트폴리오 노출` 체크박스를 true로 설정한다.
- 나머지는 false로 명시적으로 재설정한다(이전 실행에서 체크된 게 남아있지 않도록).

### FR-7. 커버이미지 자동 채움
- `커버이미지` 속성에 `https://opengraph.githubassets.com/1/{owner}/{repo}` 외부 URL을 파일로 등록한다.

### FR-8. 차트/갤러리 뷰 구성
- **차트 뷰 1 — 기술태그 분포**: `기술태그` multi_select 기준 막대 차트.
- **차트 뷰 2 — 활동상태 비율**: `활동상태` select 기준 도넛/파이 차트.
- **갤러리 뷰 — 포트폴리오**: `포트폴리오 노출 = true` 필터, 카드 커버로 `커버이미지` 사용, `마지막푸시일` 내림차순 정렬.
- 뷰 이름은 각각 "기술태그 분포", "활동상태 비율", "포트폴리오 갤러리"로 통일한다(재실행 시 동일 이름 뷰 재사용/중복 생성 방지).

### FR-9. 완료 알림 이메일
- 모든 저장소 처리가 끝나면 Resend MCP로 `hwamokworks@gmail.com`에 요약 메일을 발송한다.
- 메일 본문 최소 포함 항목: 처리한 저장소 수, 활발함/방치됨 개수, 포트폴리오 노출로 선정된 5개 저장소명 + 링크, 실패/스킵된 저장소가 있다면 목록, Notion DB 링크.
- 이메일 발송은 **모든 저장소 처리가 끝난 마지막 단계**에서 정확히 1회만 실행한다(저장소별 반복 발송 금지).

### FR-10. 슬래시 명령 사양
- 이름: `/sync-repos`
- 선택 인자: `--limit <N>`(기본 15), `--active-days <N>`(기본 90)
- 실행 주체: Claude Code 세션 (사람이 명령만 입력, 이후 전 과정 자동 진행)
- 사용 도구: GitHub MCP(저장소 목록/README/트리/languages 조회), Notion MCP(DB 조회·upsert·페이지 본문 작성·뷰 생성), Resend MCP(메일 발송)
- 실행 중 진행 상황을 단계별로 사용자에게 짧게 보고한다(예: "저장소 12/15 처리 중").

## 5. 사전 조건 / 리스크

| 항목 | 상태 | 비고 |
|---|---|---|
| GitHub MCP | 연결됨 | `claude mcp list`에서 Connected 확인 |
| Notion MCP | 연결됨 | 인증 완료, 대상 DB 생성 완료 |
| Resend MCP | **미연결** | 실행 전 `claude mcp add`로 추가 및 API 키 인증 필요. 발신 도메인/주소가 Resend에 이미 검증되어 있어야 실제 메일 발송 가능 |
| GitHub API 사용량 제한 | 낮은 위험 | 저장소 수를 15개로 제한해 요청 수를 억제 |
| Notion 차트 뷰 지원 | 확인 필요 | 워크스페이스 플랜에 따라 차트 뷰 기능 제공 여부가 다를 수 있음 — 실행 시 미지원이면 표(테이블) 뷰 + 안내 문구로 대체 |
| Mermaid 다이어그램 렌더링 | 낮은 위험 | Notion이 코드블록을 있는 그대로 보여줄 수 있음(워크스페이스에 따라 실제 다이어그램 렌더링 미지원 가능) — 코드로 남기는 것 자체가 목표이므로 실행엔 지장 없음 |

## 6. 3시간 타임박스 계획

| 시간 | 작업 |
|---|---|
| 0:00–0:20 | Resend MCP 연결/인증, 기존 Notion DB·GitHub 연결 상태 재확인 |
| 0:20–1:00 | `/sync-repos` 프롬프트 초안 작성 및 저장소 3~5개로 소규모 시험 실행 |
| 1:00–1:40 | README/구조 리포트 품질 확인 및 프롬프트 보정, 활동 분류·포트폴리오 선정 로직 검증 |
| 1:40–2:10 | 차트 뷰 2개 + 갤러리 뷰 1개 구성 |
| 2:10–2:30 | Resend 이메일 발송 연동 및 실제 수신 확인 |
| 2:30–2:50 | 전체 저장소(최대 15개) 대상 풀 실행 |
| 2:50–3:00 | 결과 점검, Definition of Done 체크리스트 확인 |

## 7. 향후 확장 아이디어 (이번 범위 밖)
- Private 저장소 지원 및 조직(org) 저장소 포함 여부 옵션화
- 정기 자동 동기화(스케줄링)
- 저장소별 커밋 활동 그래프까지 포함한 더 정교한 활동 점수화
- 실패한 저장소 자동 재시도 및 원인별 알림 세분화
