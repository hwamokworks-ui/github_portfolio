# PRD: GitHub → Notion → Build Log 포트폴리오 파이프라인

- 작성일: 2026-08-05 (최종 갱신: 2026-08-19 — `/sync-repos`·`/build-log` 두 PRD를 하나로 합침. 실행 절차 상세는 각각 `.claude/commands/sync-repos.md`, `.claude/commands/build-log.md`, `DESIGN.md`가 원본이며, 이 문서는 핵심 동작 요약과 배경·DoD·Out of Scope·리스크·변경이력만 다룬다)
- 실행 방식: 코드 작성 없음(단, `/build-log`의 이미지 압축·HTML 조립용 Bash/Python 스크립트는 예외). GitHub MCP · Notion MCP · Vercel MCP · Resend MCP 도구 호출 + 프롬프트(자연어 지시)만으로 동작하는 두 개의 재사용 가능한 슬래시 명령
- 관련 산출물: Notion 데이터베이스 "GitHub 리포지토리 관리"(부모 페이지: `https://app.notion.com/p/3b34a3b69a328016810bd2147f3222c0`, 데이터소스 `collection://e47e4d32-3ee6-49c8-a343-1c7ab4f1159a`), "Build Log" Artifact + 배포용 `index.html`

---

## 1. 배경 및 문제

GitHub에 흩어진 개인 저장소들을 한눈에 파악할 방법이 없다. 어떤 저장소가 포트폴리오로 내세울 만큼 활발한지, 어떤 저장소는 방치되어 있는지, 기술 스택은 무엇인지를 저장소 하나하나 열어보지 않고는 알 수 없다. 이 문제를 두 단계 파이프라인으로 푼다.

1. **`/sync-repos`**: GitHub 저장소 전체를 Notion 데이터베이스로 자동 정리하고, 저장소별 요약 리포트와 시각화(차트/갤러리)까지 한 번의 명령으로 구성한다.
2. **`/build-log`**: `/sync-repos`가 채워둔 Notion 데이터를 재료 삼아, 실제 배포된(LIVE) 저장소만 골라 커버이미지·기술스택·README 요약·실배포 링크가 담긴 카드형 웹페이지("Build Log")를 만들어 외부에 공유한다.

두 명령은 역할이 분리되어 있다 — `/sync-repos`만 Notion에 쓰기 작업을 하고, `/build-log`는 Notion·GitHub를 읽기만 해서 정적 페이지를 만든다.

## 2. `/sync-repos` — GitHub → Notion 동기화

### 핵심 동작 요약
상세 절차는 `.claude/commands/sync-repos.md`가 원본이며, 아래는 실행 흐름 기준 요약이다.

1. 인증된 GitHub 계정의 전체 저장소(public+private, fork 포함)를 페이지네이션 끝까지, `pushed_at` 내림차순으로 조회한다.
2. `저장소명`을 중복 방지 키로 써서 이미 있는 행은 건너뛰고, 없는 것만 신규 생성한다 — 신규 저장소의 `포트폴리오 노출`은 항상 `false`로 시작한다. `설명`과 `Vercel URL`만은 기존 행이어도 재실행마다 최신값으로 덮어쓴다.
3. README + 폴더 구조를 근거로 [기술스택]/[추정 핵심기능]/[구조 관찰]/[구조 다이어그램(Mermaid)] 4개 섹션을 페이지 본문에 작성하고, 기술태그(multi_select)에 반영한다.
4. `마지막푸시일` 기준 6개월 경과 여부로 `방치됨`/`활발함`을 판정한다.
5. 기존 저장소의 노출 여부·수동 정렬 순서는 절대 되돌리지 않는다(GitHub 원본이 삭제 확인된 경우만 예외).
6. GitHub OG 이미지 URL을 파일 속성과 페이지 네이티브 커버 양쪽에 등록해야 갤러리 카드 미리보기가 실제로 뜬다.
7. 재실행할 때마다 전체 저장소를 Vercel 프로젝트 목록과 다시 대조해 `Vercel URL`을 최신화한다.
8. 갤러리 뷰 1개 + 차트 뷰 2개를 이름 기준으로 존재 확인 후 없으면만 생성하고, 부모 페이지의 "포트폴리오 요약" 섹션을 매번 갱신한다.
9. 10개 초과 시 배치 처리, Notion 429는 배치당 최대 3회 재시도, 마지막에 Resend로 요약 메일을 1회 발송한다.

### 성공 지표 (Definition of Done)
- [ ] Notion DB에 대상 저장소 수만큼 행이 생성되어 있다 (재실행 시 이미 있는 저장소명은 새로 만들지 않는다).
- [ ] 각 행 본문에 기술스택/추정 핵심기능/구조 관찰/구조 다이어그램 4개 섹션이 채워져 있다.
- [ ] `활동상태`가 6개월 기준으로 자동 채워져 있다.
- [ ] Vercel에 실제 배포된 저장소는 `Vercel URL`에 프로덕션 도메인이 채워져 있다(재실행마다 전체 재확인).
- [ ] 기존 저장소의 `포트폴리오 노출`·카드 순서는 재실행 전후로 변하지 않는다. 신규 저장소는 노출 미체크 상태로 맨 끝에 추가된다.
- [ ] DB에 차트 뷰 2개, 갤러리 뷰 1개가 존재한다.
- [ ] 실행 완료 후 요약 완료 메일이 1통 수신된다.

### Out of Scope
- Fork 저장소 자동 제외/포함 정책 세분화, 조직(org) 소유 저장소 포함 옵션화(현재 개인 계정만)
- 서브 디렉토리 전체 재귀 스캔, 코드 정적 분석(언어 비율은 GitHub API `languages`로 대체)
- 자동 스케줄 실행(cron), 저장소별 커밋 활동 그래프까지 포함한 정교한 활동 점수화
- Notion 커버이미지 직접 업로드(외부 URL 참조로 대체), Resend 발신 도메인 신규 설정
- 다국어 리포트, 저장소별 커스텀 프롬프트 튜닝, 배치당 3회 이상의 정교한 재시도/백오프
- 커스텀 도메인까지 포함한 Vercel 배포 판정(기본 `*.vercel.app`만 신뢰)

### 리스크
| 항목 | 상태 | 비고 |
|---|---|---|
| GitHub / Notion MCP | 연결됨 | 매 실행 사전 점검 |
| Vercel MCP | 연결 확인 필요 | 프로젝트 0개면 다른 계정 연결 의심 — 재인증(`/mcp`) 요청 후 대기 |
| Resend MCP | 연결 필수 | 연결 안 되면 실행 자체를 중단(메일 발송이 마지막 단계 필수) |
| GitHub API 사용량 제한 | 낮은 위험 | 페이지네이션 + `minimal_output`/`Grep` 조합으로 토큰 한도 관리 |
| Notion 차트 뷰 지원 | 확인 필요 | 워크스페이스 플랜에 따라 미지원 시 표 뷰로 대체 |
| Mermaid 렌더링 | 낮은 위험 | 워크스페이스가 실제 렌더링 미지원이어도 코드로 남기는 것 자체가 목표 |

### 변경 이력
최초 MVP("public만, 상위 15개, 90일 기준, 상위 5개만 노출, URL 기준 upsert")에서 다음과 같이 바뀌었다.
- 전체 저장소(private 포함) 전량 조회로 확장
- 활동 분류 기준 90일 → **6개월**
- 중복 방지 키 URL → **저장소명 기준 skip-if-exists**
- **Vercel 배포 확인**이 새 핵심 기능으로 추가
- 포트폴리오 노출 정책 "전체 자동 체크" → **"기존 값 보존 + 신규만 미체크로 추가"**

## 3. `/build-log` — 카드형 포트폴리오 페이지

### 핵심 동작 요약
상세 절차는 `.claude/commands/build-log.md`(콘텐츠·데이터 규칙)와 `DESIGN.md`(디자인 토큰·컴포넌트 스타일)가 원본이며, 아래는 실행 흐름 기준 요약이다.

1. 기존 `index.html`의 카드 데이터(저장소 집합 + 설명·태그·pushed 날짜·Vercel URL 등 필드 값)를 최신 Notion 데이터와 비교한다. 완전히 동일하면 재생성 여부만 확인하고 끝낸다. 신규 저장소가 있거나 기존 저장소의 필드 값만 바뀐 경우(신규 저장소 없이도 해당) 모두 사용자에게 **전체 재생성**(LIVE 전체 README·커버 이미지 재조회) vs **증분 업데이트**(신규 저장소만 README·커버 이미지 새로 조회하고, 기존 카드는 필드 값만 최신화해 재사용, 통계·필터는 전체 기준으로 재계산) 중 무엇을 할지 먼저 물어본다.
2. Notion "카드 순서 기준" 뷰(정렬 없는 테이블 뷰)를 view 모드로 한 번 조회해 전체 데이터·카드 순서·통계 집계 대상을 동시에 확보한다(SQL 재정렬 금지).
3. `포트폴리오 노출=false`는 완전히 제외, `노출=true`+`Vercel URL` 있음은 **LIVE**, `노출=true`+`Vercel URL` 없음은 **미배포**(최종 보고 집계에만 반영). 순서는 조회 응답 순서를 그대로 따른다.
4. (전체 재생성=LIVE 전체, 증분 업데이트=신규 저장소만) README를 가져와 첫 `#` 제목(괄호 속 영문 제거)을 카드/모달 제목으로 쓴다.
5. 같은 대상 저장소의 GitHub OG 이미지를 640px·JPEG q68로 압축해 fragment본에 base64로 임베드한다(Artifact CSP 우회).
6. Pretendard 웹폰트 파일은 임베드하지 않고 시스템 폰트 폴백만 지정한다(`DESIGN.md` 기준).
7. 포트폴리오/통계 2탭 구성 — 기술 스택 필터 칩 + 커버 이미지 토글(기본 꺼짐) + 카드 그리드, 클릭 시 모달(커버 제외, README 렌더링). 통계 탭은 `포트폴리오 노출=true` 저장소 전체(배포 여부 무관)를 기준으로 집계한다.
8. fragment본은 Artifact로 발행(기존 URL 갱신), 완전한 문서본은 `<meta charset="UTF-8">` 포함해 프로젝트 루트 `index.html`로 저장한다.
9. 저장 후 GitHub에 커밋·푸시할지 사용자에게 확인하고, 동의한 경우에만 커밋·푸시한다.

### 성공 지표 (Definition of Done)
- [ ] `포트폴리오 노출=true` + `Vercel URL` 있음 조건의 저장소를 **빠짐없이** 카드로 표시한다.
- [ ] `포트폴리오 노출=false`인 저장소는 배포 여부와 무관하게 완전히 제외된다.
- [ ] 각 카드에 커버이미지, README 기준 제목, 설명(있으면), 기술 태그, LIVE 배지, Live/GitHub 링크, pushed 날짜를 보여준다.
- [ ] 카드 순서가 지정된 Notion 뷰의 행 순서와 정확히 일치한다.
- [ ] Artifact 공유 URL이 생성되고, 재실행 시 같은 URL이 갱신된다.
- [ ] `index.html`이 base64 폰트 없이 가벼운 상태로 저장된다.
- [ ] 제작 과정에서 Vercel MCP를 전혀 호출하지 않는다.

### Out of Scope
- Notion 쓰기 작업 일체(조회만 — `/sync-repos`와 역할 분리)
- Vercel MCP 직접 호출을 통한 실시간 배포 상태 재확인(Notion의 `Vercel URL`을 신뢰)
- 저장소 내 `cover.*` 파일 탐색(폐기됨 — 항상 GitHub OG 이미지만 사용)
- GitHub 변경 감지 자동 재생성 파이프라인, 다크 테마 자동 전환
- 커스텀 도메인 연결 여부 확인, 모바일 앱(Expo) 등 Vercel과 무관한 배포 형태 포함
- `index.html`의 실제 Vercel 배포(push 등)는 별도 요청 시 수행 — 파일 생성까지가 이 명령의 책임

### 리스크
| 항목 | 내용 |
|---|---|
| Notion 의존성 | `/sync-repos`가 먼저 실행되어 데이터가 정확해야 한다. 실행 안 했거나 낡았으면 이 페이지도 낡은 정보를 보여준다 |
| 신규 저장소는 기본 미노출 | 사용자가 Notion에서 직접 체크하기 전까지 `/build-log`를 재실행해도 나타나지 않는다 |
| 카드 순서 기준 뷰 삭제/변경 위험 | 뷰 URL이 삭제되거나 필터가 추가되면 순서 조회가 실패하거나 의도와 다른 결과가 나올 수 있다 |
| 정적 스냅샷 | Artifact와 `index.html` 모두 실행 시점 데이터로 고정 — Notion이 바뀌면 재실행해야 최신화된다 |
| README 렌더링 한계 | 자체 경량 마크다운 변환기라 복잡한 GFM 문법 일부가 깨질 수 있다. 이미지는 `[이미지: alt]`로 대체 |
| 429 (OG 이미지) | 짧은 시간에 몰리면 실패할 수 있어 재시도가 필요하다 |

### 변경 이력
최초 설계("GitHub MCP만 사용, `homepage` 필드로 배포 판정, 저장소 내 `cover.*` 우선 탐색")에서 다음과 같이 바뀌었다.
- 데이터 출처를 **GitHub 메타데이터 추론 → Notion DB 신뢰**로 전환(이중 작업 제거)
- `cover.*` 탐색 폐기, **GitHub OG 이미지만 사용**
- Pretendard base64 임베드(2MB+ 증가) 폐기, **시스템 폰트 폴백**으로 전환
- 카드 제목을 저장소명 → **README 첫 `#` 제목 기준**으로 변경
- 2026-08-19: 색상·타이포그래피·컴포넌트 스타일을 `DESIGN.md`로 분리해 재사용 가능하게 함
- 2026-08-19: 매번 전체 재생성하던 방식을 개선 — 실행 전 기존 카드 데이터와 최신 Notion 데이터를 비교(저장소 집합뿐 아니라 설명·태그·pushed 날짜·Vercel URL 등 필드 값까지)해 완전히 동일하면 재생성 여부만 묻고, 뭔가 바뀌었으면 전체 재생성/증분 업데이트(신규 저장소만 README·커버 이미지 조회, 기존 카드는 필드만 갱신) 중 선택하도록 질문 추가. 산출물 저장 후 GitHub 커밋·푸시 여부도 매번 확인하도록 변경(자동 커밋 금지)

## 4. 문서 이력
- 2026-08-19: `/sync-repos`(`prd.md`)와 `/build-log`(`prd-build-log.md`) 두 개로 나뉘어 있던 PRD를 이 문서 하나로 합침. 실행 로직과 문서가 따로 갱신되며 어긋나는 문제를 막기 위해, 각 명령의 FR 상세는 해당 command 파일(및 `/build-log`의 경우 `DESIGN.md`)을 원본으로 삼고 이 문서는 배경·요약·DoD·Out of Scope·리스크·변경이력만 유지한다.
